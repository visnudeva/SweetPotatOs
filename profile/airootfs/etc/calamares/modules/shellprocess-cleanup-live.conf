---
# Remove live-only installer UI and ensure the installed user can sudo.
dontChroot: false
timeout: 60
verbose: true

script:
  - "-/usr/local/bin/sweetpotatos-cleanup-live '${USER}'"

i18n:
  name: "Cleaning up live installer"
