---
# Remove live-only installer UI and re-assert sudo for the installed user.
dontChroot: false
timeout: 60
verbose: true

script:
  # No leading "-" -- sudo setup must not be silently skipped.
  - "/usr/local/bin/sweetpotatos-cleanup-live '${USER}'"

i18n:
  name: "Cleaning up live installer"
