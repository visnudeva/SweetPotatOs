---
description: SweetPotatOs project memory — read NOTES.md; theme sync, fastfetch, lid sleep
alwaysApply: true
---

# SweetPotatOs context

Before non-trivial changes, read `NOTES.md` at the repo root.

## Hard rules from prior work

- Theme source of truth is the sibling **SweetPotato** repo. Sync into this ISO with `./sync-theme.sh` (not by hand-copying one tree only). On a GNOME/Bluefin host, `sync-theme.sh` must **not** copy Papirus/glycin into `$HOME` (that blanks half the GNOME icons). It skips live home automatically; never pass `--live` on this host.
- Desktop compositor is **Swirl**; `sway` package stays for swaybar/swaymsg/swaynag. User config is `~/.config/swirl/` only — do **not** also ship `~/.config/sway/` (Swirl checks sway first). Keep `include /etc/sway/config.d/*`.
- Fastfetch primary logo is colored `SPLogo.png` (`logo.type: auto`). Do not switch primary back to monochrome `SPLogo.asc`.
- Lid close suspends via logind `lid-sleep.conf`. Never reintroduce `do-not-suspend.conf` that sets `HandleLidSwitch=ignore`. Caffeine must not block lid suspend.
- Ly always lists root first; never ship `/etc/ly/save.txt` as `0`. Post-install `sweetpotatos-fix-ly` (Calamares `shellprocess@fix-ly`) must set the saved user to `${USER}` and remove `liveuser`.
- Installed user sudo: `sweetpotatos-fix-sudo` (via `fix-ly` + `cleanup-live`) must add `${USER}` to `wheel`, write `/etc/sudoers.d/10-installed-user` (explicit user rule) and `/etc/sudoers.d/10-wheel`. Never rely only on Calamares `10-installer` or a commented `%wheel` line in `/etc/sudoers`.
- After install, `shellprocess@cleanup-live` must remove Install SweetPotatOs / calamares `.desktop` leftovers from the app launcher.
- Default terminal is **foot** (opaque); do not switch `Mod+Return` to kitty without updating skel + SweetPotato + sync. Do not ship `~/.config/kitty/`.
- Ly `default_input = password`. Enable `bluetooth.service` on live/install.
- Wallpaper: `ensure-wallpaper.sh` must not overwrite a saved `wallpaper.conf` with the UsefulBinds fallback.
- Live Calamares keyboard needs `sweetpotatos-sway-xkb-watch` (locale1 → Swirl). Do not assume Calamares Wayland apply changes the compositor by itself.
- Push SweetPotatOs to **both** `origin` (GitHub) and `sourceforge` when the user asks to publish git. ISO upload is separate (`sourceforge/upload.sh`).
